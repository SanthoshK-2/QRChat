# QR Chat Project
A Node.js based chat application.

## Setup Instructions
1. Clone the repository.
2. Run `npm install` in both the client and server directories.
3. Start the backend using `node server.js` (or your specific start command).

## Features
* QR-based connectivity
* Real-time messaging
*